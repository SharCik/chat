    <div class="navbar navbar-default navbar-fixed-top" style="background-color: #33b5e5">
      <div class="container">
        <div class="navbar-header">
          <span class="navbar-brand"><%= link_to "Chat", root_path, style:"color: #fff;" %></span>
          <button class="navbar-toggle" type="button" data-toggle="collapse" data-target="#navbar-main">
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>
        </div>
        <div class="navbar-collapse collapse" id="navbar-main">
        
          <ul class="nav navbar-nav navbar-right">
          <% if user_signed_in? %>
            <li><%= link_to "Profile, " + current_user.name, user_path(current_user.id), style:"color: #fff;" %></li>
            <li style="color: #fff;"><%= link_to "Sign out",  destroy_user_session_path, :method => :delete, style:"color: #fff;" %></li>
          <% else %>
            <li><%= link_to "Sign in", new_user_session_path , style:"color: #fff;" %></li>
            <li><%= link_to "Sign up", new_user_registration_path , style:"color: #fff;" %></li>
          <% end %>
          </ul>
        
        </div>
      </div>
    </div>